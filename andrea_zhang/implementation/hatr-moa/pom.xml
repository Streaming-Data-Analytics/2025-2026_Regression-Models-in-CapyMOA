<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>moa.classifiers.trees</groupId>
    <artifactId>hoeffding-adaptive-tree-regressor-moa</artifactId>
    <version>1.0-SNAPSHOT</version>
    <packaging>jar</packaging>
    <description>HATR native MOA implementation — CapyMOA compatible</description>

    <properties>
        <maven.compiler.release>11</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    </properties>

    <!--
        MOA is not on Maven Central. Install it locally first:
          mvn install:install-file -Dfile=moa.jar \
            -DgroupId=nz.ac.waikato.cms.moa \
            -DartifactId=moa -Dversion=2024.07.0 \
            -Dpackaging=jar

        CapyMOA users: the moa.jar is at $(python -c "import capymoa; print(capymoa.__file__)")/../../moa.jar
        or install CapyMOA and run: python -c "from capymoa.env import _get_moa_jar; print(_get_moa_jar())"
    -->
    <dependencies>
        <dependency>
            <groupId>nz.ac.waikato.cms.moa</groupId>
            <artifactId>moa</artifactId>
            <version>2024.07.0</version>
            <scope>provided</scope>
        </dependency>
        <!--
            Apache Commons Math — used for the standard-normal CDF (Erf.erfc) in the
            alternate-tree z-test. Scope is "provided" because the MOA runtime jar
            already bundles org.apache.commons.math3 (verified: Erf.class present),
            so it must not be shaded into this jar.
        -->
        <dependency>
            <groupId>org.apache.commons</groupId>
            <artifactId>commons-math3</artifactId>
            <version>3.6.1</version>
            <scope>provided</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <version>3.3.0</version>
            </plugin>
        </plugins>
    </build>
</project>
